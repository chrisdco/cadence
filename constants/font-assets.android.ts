/** Android links Google Sans Flex Rounded at build time through the expo-font
 * config plugin (see `android.fonts` in app.json), which registers the whole
 * weight family under one name. Nothing loads at runtime. */
export const fontAssets = {};
